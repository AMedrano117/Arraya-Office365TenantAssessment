# Copy this file to envs/<client-name>.env and fill in the values.
# Run with: python cli.py full --env <client-name>
#
# All values here are overridden by explicit CLI flags if provided.

AUTH_MODE=Certificate
TENANT_ID=
CLIENT_ID=
CERT_THUMBPRINT=
# CLIENT_SECRET=   (only for ClientSecret auth mode)
MODE=python
